name: gamepad
description: Cross-platform gamepad input for Dart. Pure Dart, no Flutter dependency.
version: 0.1.0
repository: https://github.com/repentsinner/gamepad

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  ffi: ^2.2.0
  code_assets: ^1.0.0
  hooks: ^1.0.2
  native_toolchain_c: ^0.17.5

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
